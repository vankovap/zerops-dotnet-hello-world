zerops:
- setup: helloworld
  build:
    base: dotnet@6
    buildCommands:
      - dotnet publish -c Release -o app
    deploy:
      - ./app/~
  run:
      start: dotnet ./zerops-dotnet-hello-world.dll
